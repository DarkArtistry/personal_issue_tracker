// NOTE: Usually i will get the configuration from the server. But for
// simplicity i will use this file as the configuration

export const config = {
  issueTypes: ['bug', 'improvement', 'question']
}
